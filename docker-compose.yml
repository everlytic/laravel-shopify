version: "3"
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        working_dir: /var/www/
        container_name: laravel-shopify
        volumes:
            - ./:/var/www

